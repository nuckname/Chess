using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanTake : MonoBehaviour
{
    //can a piece be taken.
    //
    private ObjectClicker getColorPosOnClick;
    private bool PieceClickedColor;


    //use gameObject can replace it with takeCircle;
    private void CanTakePiece()
    {
        //takes in hit array.
    }

    private void ColorCalculator(bool PieceClickedColor)
    {
        if (PieceClickedColor)
        {
            
        }

        else
        {

        }
    }

}
